# FairyGUI-MCP-Headless 协作规则

## 沟通语言

- 始终使用中文与开发者沟通。

## Git 提交规范

- Git 提交说明必须使用中文。
- 提交说明采用 Conventional Commits 风格：`<类型>: <中文摘要>`。
- 示例：`feat: 新增组件查询能力`、`fix: 修复图集资源解析错误`、`test: 补充选择器边界测试`。
- 每次提交只包含一项完整、可验证的小功能，不得混入无关改动。

## 开发模式

- 开始开发或调整前，必须先向开发者确认本次采用“开发模式”还是“调整模式”；开发者已经明确指定时不得重复询问。
- **开发模式**：按既定目标持续开发。每完成一项完整、可验证的小功能，必须立即创建一条 Git 提交记录。
- **调整模式**：由开发者持续检查并提出调整意见。在开发者明确确认完成前不得创建 Git 提交记录；确认后再提交本轮全部已确认改动。

## 接口、测试与实现顺序

- 开发任何功能时，必须依次执行：
  1. 明确接口、输入输出、错误语义与验收条件。
  2. 编写或补充单元测试。
  3. 实现代码。
  4. 运行相关单元测试，并确认全部通过。
  5. 按当前开发模式决定是否创建 Git 提交。
- 单元测试未通过时，禁止提交对应功能。
- 禁止仅为了让实现通过测试而直接修改既有单元测试。
- 只有能够证明既有测试描述的行为已不适用于当前版本时，才允许修改该测试；修改时必须同步说明失效原因、新预期和依据。
- `test:implemented` 是提交门禁，所有已实现能力必须通过。`test:future` 仅用于展示 planned 契约，不得把已实现失败项改标为 planned 或 skip。

## 跨平台约束

- Windows 是 V1 唯一正式开发、测试和截图基线平台。
- 不主动适配 Linux 或 macOS，但不得引入 Windows 专属依赖、硬编码盘符、反斜杠拼接路径或平台专用业务逻辑。
- 文件路径必须使用 Node.js `path`、URL 或 POSIX 逻辑路径 API 构造。

